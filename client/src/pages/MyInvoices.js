import React, { useEffect } from "react";
import { Typography } from "@mui/material";

const MyInvoices = () => {
  return <Typography variant="body">MyInvoices.</Typography>;
};

export default MyInvoices;
